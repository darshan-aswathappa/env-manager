# .envVault — environment configuration example
# Copy this file to .env and fill in your values.
# Never commit .env to source control.

# Example variables (this app has no required env config for development)
# ENVVAULT_TELEMETRY_ENABLED=false
